#pragma once

#include <usbspec.h>

#define LEN_USB_DEVICE_DESCRIPTOR	sizeof(struct _USB_DEVICE_DESCRIPTOR) // 18

#define LEN_USB_CONFIGURATION_DESCRIPTOR	sizeof(struct _USB_CONFIGURATION_DESCRIPTOR) // 9
